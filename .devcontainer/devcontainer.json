{
  "name": "Oh My OpenAgent Secure Dev",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "remoteUser": "vscode",
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspaces/${localWorkspaceFolderBasename},type=bind,consistency=cached",
  "mounts": [
    "source=omo-dev-config,target=/home/vscode/.config,type=volume",
    "source=omo-dev-cache,target=/home/vscode/.cache,type=volume"
  ],
  "runArgs": [
    "--cap-drop=ALL",
    "--security-opt=no-new-privileges",
    "--pids-limit=512"
  ],
  "containerEnv": {
    "OMO_DISABLE_POSTHOG": "1",
    "OMO_SEND_ANONYMOUS_TELEMETRY": "0",
    "XDG_CACHE_HOME": "/tmp/vscode-cache"
  },
  "postCreateCommand": "bash /workspaces/${localWorkspaceFolderBasename}/scripts/bootstrap-oh-my-openagent.sh",
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash",
        "security.workspace.trust.enabled": true
      }
    }
  }
}
